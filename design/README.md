# Design references

`figma-export/` is retained as a design/reference artifact, not as the final UI implementation. The approved correction is lower density, larger hit areas, volume/playback/random sliders, and knobs mainly for pan/FX.
