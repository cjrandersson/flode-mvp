
  # High-Fidelity Audio Instrument UI

  This is a code bundle for High-Fidelity Audio Instrument UI. The original project is available at https://www.figma.com/design/CFeWEy5UMCbDmBR8lsY7sH/High-Fidelity-Audio-Instrument-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  