# Historical path notice

The active Max material has been organised under `max/`:

- `max/current/` — current implementation candidate
- `max/core-phase-01/` — separate reusable six-pod experiment
- `max/experiments/pod-a/` — earlier Pod A build progression
- `reference/jungulator/patches/` — reconstructed lineage material

Keep this file so links from the original starter package do not fail. Do not place new builds here.
